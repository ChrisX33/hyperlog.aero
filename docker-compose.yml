services:
  web:
    build: .
    container_name: hyperlog-website
    env_file:
      - .env
    networks:
      - hyperlog_prod
      - jetlink_internal
    restart: unless-stopped

networks:
  hyperlog_prod:
    external: true
  jetlink_internal:
    external: true
