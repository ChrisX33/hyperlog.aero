---
import '../styles/global.css';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="HyperLog - Electronic Pilot Credentials and Blockchain Logbook Infrastructure. Briefing by JetLink Technologies Ltd." />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <title>HyperLog - IATA Briefing</title>
  <style>
    /* Print: each slide becomes one page */
    @media print {
      @page {
        size: 297mm 210mm;
        margin: 10mm 14mm;
      }

      html, body {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
        margin: 0 !important;
        padding: 0 !important;
        width: 100%;
      }

      body {
        font-size: 85%;
      }

      .pitch-slide {
        break-after: page;
        break-inside: avoid;
        page-break-after: always;
        page-break-inside: avoid;
        height: 190mm;
        max-height: 190mm;
        width: 269mm;
        overflow: hidden;
        box-sizing: border-box;
        position: relative;
      }

      .pitch-slide:last-child {
        break-after: auto;
        page-break-after: auto;
      }

      .no-print {
        display: none !important;
      }

      /* Tighten spacing for print */
      .pitch-slide .mb-10 {
        margin-bottom: 1.25rem !important;
      }

      .pitch-slide .mb-8 {
        margin-bottom: 1rem !important;
      }

      .pitch-slide .py-16 {
        padding-top: 0 !important;
        padding-bottom: 0 !important;
      }

      .pitch-slide .gap-8 {
        gap: 0.75rem !important;
      }

      .pitch-slide .gap-6 {
        gap: 0.5rem !important;
      }

      .pitch-slide .gap-4 {
        gap: 0.35rem !important;
      }

      .pitch-slide .space-y-4 > * + * {
        margin-top: 0.35rem !important;
      }

      .pitch-slide p {
        line-height: 1.4 !important;
      }

      .pitch-slide .p-6 {
        padding: 0.75rem !important;
      }

      .pitch-slide .p-5 {
        padding: 0.6rem !important;
      }
    }

    /* Screen: scrollable slides */
    @media screen {
      .pitch-slide {
        min-height: 100vh;
      }

      .print-only {
        display: none !important;
      }
    }
  </style>
</head>
<body class="bg-surface-base text-on-surface">
  <!-- Save as PDF button -->
  <button
    onclick="window.print()"
    class="no-print fixed top-6 right-6 z-50 flex items-center gap-2 px-5 py-2.5 bg-primary text-on-primary font-bold text-xs tracking-widest uppercase rounded-md hover:opacity-90 transition-opacity cursor-pointer"
  >
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
    Save as PDF
  </button>

  <slot />
</body>
</html>
